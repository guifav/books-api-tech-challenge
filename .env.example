# Flask Configuration
FLASK_ENV=production
FLASK_DEBUG=False
API_HOST=0.0.0.0
API_PORT=5005
PORT=5005

# Security
# JWT_SECRET=your-super-secret-jwt-key-here - removido (autenticação desabilitada)

# Python Path (for deployment)
PYTHONPATH=/var/task

# Optional: Database URL (if using external database)
# DATABASE_URL=postgresql://user:password@host:port/dbname

# Optional: Redis URL (if using Redis for caching)
# REDIS_URL=redis://localhost:6379/0